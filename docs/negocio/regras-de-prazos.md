# Regras de Prazos

A lógica de prazos segue o padrão da planilha:

- se real > previsto → atraso
- se previsto existe e real não → pendente
- ausência de ambos → não iniciado

Alertas e SLAs serão derivados dessas regras no futuro.
